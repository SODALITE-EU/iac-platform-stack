# These are deployment-dependent variables. They can either be hardcoded here
# or can be generated using the functions in ``load_into_k8s.sh``. If they
# are generated in ``load_into_k8s.sh``, look for the block with lots of 
# ``exports`` and add them there.

export KEYCLOAK_REALM=SODALITE
export KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET}
export KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD}
export KEYCLOAK_CLIENT_ID=sodalite-ide
export VAULT_ROOT_TOKEN=${VAULT_ROOT_TOKEN}
export GRAFANA_ADMIN_USER=admin
export GRAFANA_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}