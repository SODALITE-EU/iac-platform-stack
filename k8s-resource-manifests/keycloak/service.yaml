---
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: sodalite-services
spec:
  selector:
    app.kubernetes.io/name: keycloak
  ports:
  - port: 8100
    targetPort: http
    protocol: TCP
    name: http
  - port: 8443
    targetPort: https
    protocol: TCP
    name: https