---
apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: sodalite-services
spec:
  selector:
    app.kubernetes.io/name: whoami
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http